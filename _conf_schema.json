{
  "sleep_window_start": {
    "type": "string",
    "description": "入睡窗口起点（开始渐困）",
    "default": "23:00",
    "hint": "格式 HH:MM。过了这一刻起 LLM 回复会自然变短变黏糊，到窗口终点仍未睡则强制切 SLEEPING（语义：困得不行睡着了）"
  },
  "sleep_window_end": {
    "type": "string",
    "description": "入睡窗口终点（兜底强制入睡）",
    "default": "00:00",
    "hint": "格式 HH:MM。晚于 sleep_window_start 即跨午夜（典型用法）；不晚于则窗口不跨天"
  },
  "wake_window_start": {
    "type": "string",
    "description": "起床窗口起点（最早自然醒）",
    "default": "07:30",
    "hint": "格式 HH:MM。每天会在 [起点, 终点] 内随机选一个时刻醒来（持久化，重启不变，跨过凌晨重新随机）"
  },
  "wake_window_end": {
    "type": "string",
    "description": "起床窗口终点（最晚自然醒）",
    "default": "08:30",
    "hint": "格式 HH:MM。中雨以上可在窗口内提前被雨声吵醒（默认走半醒→清醒，不直接清醒）"
  },
  "semi_awake_window": {
    "type": "int",
    "description": "半醒窗口（分钟）",
    "default": 30,
    "hint": "起床后多少分钟内处于半醒状态，会自主发起消息（半醒活动 / 梦境生成）"
  },
  "enable_dream": {
    "type": "bool",
    "description": "启用梦境生成",
    "default": true,
    "hint": "半醒期间会调用配置的模型生成梦境"
  },
  "dream_delay_minutes": {
    "type": "int",
    "description": "梦境生成延迟（分钟）",
    "default": 30,
    "hint": "进入 SEMI_AWAKE 后，延迟此分钟数再生成梦境，便于在半醒时回忆刚醒的内容"
  },
  "dream_provider_id": {
    "type": "string",
    "_special": "select_provider",
    "description": "梦境生成用的模型",
    "hint": "建议用免费轻量模型（如 Qwen2.5），不消耗主力 token"
  },
  "weather_provider": {
    "type": "string",
    "description": "天气数据源",
    "default": "wttr",
    "hint": "qweather=和风天气（国内服务快且稳，需免费 key，推荐）/ wttr=wttr.in 真实 API（无需 key，默认）/ mock=模拟数据（调试用，沙溪默认中雨可能触发雨声唤醒）"
  },
  "qweather_key": {
    "type": "string",
    "description": "和风天气 API Key",
    "default": "",
    "hint": "在 dev.qweather.com 免费注册 → 控制台创建项目和 Key，粘贴到这里。weather_provider 选 qweather 时必填，没填会自动回退到 wttr.in"
  },
  "location": {
    "type": "string",
    "description": "当前所在地",
    "default": "沙溪",
    "hint": "城市名或地点，例如 '沙溪'、'杭州'、'东京'；可用 /set_location 切换"
  },
  "poll_interval_minutes": {
    "type": "int",
    "description": "天气轮询间隔（分钟）",
    "default": 60,
    "hint": "默认 60 分钟。可用 /weather 手动查询绕过轮询"
  },
  "rain_wake_threshold_mm": {
    "type": "float",
    "description": "雨声唤醒阈值（mm/h）",
    "default": 2.5,
    "hint": "过去 1 小时降水量达到此值则触发雨声唤醒，2.5=中雨起点。仅在起床窗口内生效（v0.3.0 起）"
  },
  "rain_wake_pass_through_semi": {
    "type": "bool",
    "description": "雨声唤醒是否经过半醒状态",
    "default": true,
    "hint": "true=雨声吵醒后先半醒做梦再清醒（v0.3.0 默认，被吵醒不等于清醒）；false=直接清醒（不做梦）"
  },
  "hot_threshold": {
    "type": "float",
    "description": "炎热阈值（°C）",
    "default": 30.0,
    "hint": "温度高于此值触发炎热关心"
  },
  "cold_threshold": {
    "type": "float",
    "description": "寒冷阈值（°C）",
    "default": 20.0,
    "hint": "温度低于此值触发寒冷关心"
  },
  "cool_threshold": {
    "type": "float",
    "description": "凉爽阈值（°C）",
    "default": 25.0,
    "hint": "温度低于此值开始感到凉爽（20-25°C 区间）"
  }
}